<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_busStopChooser">

    <fragment
        android:id="@+id/nav_busStopChooser"
        android:name="hr.algebra.zatoninfo.BusStopSelectionFragment"
        android:label="@string/menu_bus_timetable"
        tools:layout="@layout/fragment_bus_stop_selection" >
        <action
            android:id="@+id/busStopChooserToMap"
            app:destination="@id/nav_busStopsMap"/>
    </fragment>

    <fragment
        android:id="@+id/nav_busStopsMap"
        android:name="hr.algebra.zatoninfo.BusStopsMapFragment"
        android:label="@string/menu_bus_stops"
        tools:layout="@layout/fragment_bus_stops_map">
    </fragment>

    <fragment
        android:id="@+id/nav_settings"
        android:name="hr.algebra.zatoninfo.SettingsFragment"
        android:label="@string/menu_settings">
    </fragment>
</navigation>